# Base URL de la API REST de Pulsar (FastAPI). La usan el cliente HTTP y `pnpm gen:api`.
# Copia este archivo a `.env` y ajusta el valor si tu API corre en otro host/puerto.
VITE_API_BASE_URL=http://localhost:8000
